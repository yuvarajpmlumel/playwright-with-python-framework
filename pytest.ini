# pytest.ini
[pytest]
addopts = -v --headed --browser=chromium --html=report.html --self-contained-html
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    smoke: quick sanity test
    regression: thorough checks
    ui: tests that hit the browser